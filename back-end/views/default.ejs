<!-- views应用的视图文件，在app.js中设置好视图引擎和模板之后（直接通过res.render（“文件名”渲染）），该目录即为应用视图的根目录，然后路由文件就会根据app.js中的设置加载并渲染该目录下的视图文件。 -->
{
    "code": <%= status.code %>,
    "msg": "<%= status.msg %>",
    "data": <%-  data %> 
}